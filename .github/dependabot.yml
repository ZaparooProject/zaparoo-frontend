# Zaparoo Frontend
# Copyright (c) 2026 Wizzo Pty Ltd and the Zaparoo Project contributors.
# SPDX-License-Identifier: LicenseRef-PolyForm-Noncommercial-1.0.0

version: 2
updates:
  - package-ecosystem: "cargo"
    directory: "/rust"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    # Slint is pinned exactly (`=1.18.0`) and bumped by hand together with
    # slint-tr-extractor, the deny.toml exceptions, and the notices file
    # (see AGENTS.md, "Build caching couplings"). fontique and resvg are
    # also direct dependencies of Slint's own crates and must stay on the
    # versions Slint resolves, so they move only with a Slint bump.
    ignore:
      - dependency-name: "slint"
      - dependency-name: "slint-build"
      - dependency-name: "i-slint-*"
      - dependency-name: "fontique"
      - dependency-name: "resvg"
    groups:
      tokio:
        patterns: ["tokio*"]
      tracing:
        patterns: ["tracing*"]
      serde:
        patterns: ["serde*"]

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
