# IDE
.idea/
.vscode/
*.swp
*~

# macOS
.DS_Store

# Local MiSTer drive mirror used for ad-hoc runs
.local_drv/

# Snapshots, release bundles, and dev-run logs
output/

# Editor and LSP caches
.cache/

# Local environment — secrets, never commit
.env

# AI agent tooling — local only, not shared
CLAUDE.md
.claude/
.opencode/
.pi/
docs/plans/

# Cargo build cache
rust/target/

# Dev-run log that lands in the repo root (real log goes to ~/.local/share/zaparoo/logs/)
/frontend.log

# Python bytecode from scripts/
__pycache__/
